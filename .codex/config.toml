web_search = "cached"
model_reasoning_effort = "high"
personality = "pragmatic"

[shell_environment_policy]
include_only = [
  "PATH",
  "HOME",
  "LANG",
  "LC_ALL",
  "SSH_AUTH_SOCK",
  "SSH_AGENT_PID",
  "GIT_SSH_COMMAND",
  "GIT_ASKPASS"
]

[features]
shell_snapshot = true
