"""Provider implementations for external orchestration dependencies.

Providers are concrete adapters for LLMs, search tools, and other external
systems. Core workflow code should depend on ports/protocols, not on providers.
"""
